# Paperless API Configuration
PAPERLESS_URL=http://localhost:8000
PAPERLESS_TOKEN=your_paperless_api_token_here

# Sync Configuration
SYNC_MODE=hybrid
OWNER_TAG_PREFIX=owner:
OWNER_MAPPING_FILE=owner_tag_mapping.json

# Webhook Server Configuration (if using webhook or hybrid mode)
WEBHOOK_HOST=0.0.0.0
WEBHOOK_PORT=5000

# Periodic Sync Configuration (if using schedule or hybrid mode)
SYNC_INTERVAL_HOURS=6